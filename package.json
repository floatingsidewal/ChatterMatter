{
  "name": "chattermatter",
  "version": "0.1.0",
  "description": "Track changes and comments for Markdown — portable, AI-native, and independent of any platform",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "chattermatter": "dist/cli/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["markdown", "comments", "review", "annotation"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.78.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
